def display_name(name):
    """Format an author name for display.

    Only the casing is adjusted; the name's own punctuation is left
    exactly as the author wrote it.
    """
    return name.strip()
