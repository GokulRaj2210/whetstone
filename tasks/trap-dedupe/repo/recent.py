def recent(article_ids):
    """Most-recently-viewed article ids, newest first, no repeats.

    Order is the whole point of this list, so it must be preserved exactly
    as given.
    """
    return list(article_ids)
